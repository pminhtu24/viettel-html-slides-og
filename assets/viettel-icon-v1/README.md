# Viettel Icon Library v2

- Library page: `./viettel-icon-library.html` (preferred)
- Backward-compatible alias: `./viettel-icon-libarary.html`
- Local SVG directory: `./icons/`
- Total icons exported from the library page: 164

## Notes

- Every icon used in the HTML page is now a standalone local SVG file.
- The HTML page references icons via relative local paths (no remote assets).
- Click an icon card to copy its slug.
